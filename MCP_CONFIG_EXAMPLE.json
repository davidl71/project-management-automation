{
  "mcpServers": {
    "exarp": {
      "command": "uvx",
      "args": ["exarp"],
      "description": "Project management automation - health scores, task alignment, security scanning (PyPI: pip install exarp)"
    }
  }
}
